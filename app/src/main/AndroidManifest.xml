<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.stein13.reserveit" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".newReservation"
            android:label="@string/title_activity_new_reservation" >
        </activity>
        <activity
            android:name=".aboutApp"
            android:label="@string/title_activity_about_app" >
        </activity>
        <activity
            android:name=".createDatabase"
            android:label="@string/title_activity_create_database"
            android:parentActivityName=".WelcomeActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="net.stein13.reserveit.WelcomeActivity" />
        </activity>
    </application>

</manifest>
